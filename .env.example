# Prisma Postgres en prod (base dédiée au projet, distincte de celle du site) ;
# instance locale en dev
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/va_os"

# NextAuth v5 — `openssl rand -base64 32`
AUTH_SECRET=""

# Resend (magic links + emails transactionnels)
RESEND_API_KEY=""
EMAIL_FROM="Smart Lazy VA OS <hello@example.com>"

# Stripe (phase 6 — abonnements)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
